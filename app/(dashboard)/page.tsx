import { DataGrid } from '@/components/shared/data-grid'

const Home = () => {
  return (
    <div className="container px-6 sm:px-14 flex items-center w-full h-full max-h-[300px] py-4 sm:py-8">
      <DataGrid />
    </div>
  )
}

export default Home
